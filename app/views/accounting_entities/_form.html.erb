<%= form_for(@accounting_entity) do |f| %>
  <% if @accounting_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accounting_entity.errors.count, "error") %> prohibited this accounting_entity from being saved:</h2>

      <ul>
      <% @accounting_entity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
