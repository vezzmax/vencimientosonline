<% provide(:title, @user.name) %>
<% if signed_in? %>
<!-- <p id="notice"><%= notice %></p>
 -->
<!-- <p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>
 -->
<div class="row-fluid">
  	<div class="span12">
    	<h2 class="text-info center">Estudio Contable: <strong><%= @user.accounting_entity.name %></strong>
    	</h2>
    </div>    
  	<div class="row-fluid">
  		<div class="span12"/>
  	</div>
  	<div class="row-fluid">
  		<div class="span6">
            <% if @user.directExpirations.any? %>
		      <h3>Próximos Vencimientos (<%= @user.directExpirations.count %>)</h3><br>
		      <%  @companyExpirations = @user.directExpirations
		        %>
		        <table class="table table-striped">
				  <tr>
				    <th>Impuesto</th>
				    <th>Empresa</th>
				    <th>Fecha de Vencimiento</th>
				  </tr>

				<% @companyExpirations.each do |ce| %>
				  <tr>
				    <td><%= ce.associated_tax.tax.name %></td>
				    <td><%= ce.associated_tax.company.name %></td>
				    <td><%= ce.date %></td>
				  </tr>
				<% end %>
				</table>
		    <% end %>
    	</div>
     	<div class="span6"><h3>Impuestos supervisados</h3> <br>
      		<%  @supervisions = @user.supervisions
		        %>
		        <table class="table table-striped">
				  <tr>
				    <th>Empresa</th>
				    <th>Impuesto</th>
				    <th>Nivel</th>
				  </tr>

					<% @supervisions.each do |sup| %>
					  <tr>
					    <td><%= sup.associated_tax.company.name %></td>
					    <td><%= sup.associated_tax.tax.name %></td>
					    <% if sup.level > 1 then 
					    	@badge_type = "badge-success"
					       else @badge_type = "badge-important" 
					       	end %>	
					    <td><span class="badge <%= @badge_type %>"> <%= sup.level %></span></td>
					  </tr>
					<% end %>
				</table>
  	</div>
  	<div class="row-fluid">
      <div class="span6"><h3>Últimas presentaciones</h3></div>
      <div class="span6"><h3>Vencimientos de otro nivel</h3></div>
  	</div>
</div>

<!-- 
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %> -->

<% end %>