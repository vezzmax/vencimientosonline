<%= form_for(@taxis) do |f| %>
  <% if @taxis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taxis.errors.count, "error") %> prohibited this taxis from being saved:</h2>

      <ul>
      <% @taxis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :period %><br />
    <%= f.select(:period, Tax.periods) %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.select(:category, Tax.categories) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
